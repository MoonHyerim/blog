<%= form_for(@hye) do |f| %>
  <% if @hye.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hye.errors.count, "error") %> prohibited this hye from being saved:</h2>

      <ul>
      <% @hye.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
