<p id="notice"><%= notice %></p>

<p>
  <b>Writer:</b>
  <%= @post.writer %>
</p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>
  <b>Content:</b>
  <%= @post.content %>
</p>

 <!--이 사이에다가 comment 추가
	 아래는 commant에 대한 form-->
<h2>Comments</h2>

<%= render  @post.comments%>
<!--partial/같은 view안에 comments파일의 _comment를 참고-->

<h2>Add a comment:</h2>
<%= render 'comments/form'%>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %> |
